import { publicRoutes } from './routes';
import { headerNav, headerNavImg, menuMobile, menuAction } from './nav';
import { footerLocation, footerNav, footerSocial } from './footer';
import { slider } from './slider';

const config = {
    publicRoutes,

    headerNav,
    headerNavImg,
    menuMobile,
    menuAction,

    footerLocation,
    footerNav,
    footerSocial,

    slider,
};

export default config;
